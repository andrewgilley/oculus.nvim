# Project instructions

- After making Lua edits, run the Neovim `:LuaParagraphFormat` command on the edited Lua buffers when available, then review the diff.
- Use concise, descriptive commit messages. When the user asks to commit and push, commit the intended changes and push them to the configured remote.
- Do not modify `README.md` unless the user explicitly requests it.
